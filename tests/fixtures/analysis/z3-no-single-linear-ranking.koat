# Each branch needs a conflicting direction; no single strict linear ranking exists.
(GOAL TERMINATION)
(STARTTERM (FUNCTIONSYMBOLS eval))
(VAR x)
(RULES
  eval(x) -> loop(x)
  loop(x) -> loop(x - 1) [x > 0]
  loop(x) -> loop(x + 1) [x < 0]
  loop(x) -> done(x) [x = 0]
)

